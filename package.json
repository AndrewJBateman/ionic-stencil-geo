{
  "name": "ionic-stencil-geo",
  "private": true,
  "version": "0.0.1",
  "description": "ionic-stencil-geo",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@ionic/core": "^6.0.4",
    "@stencil/core": "^2.6.0"
  },
  "dependencies": {
    "rxjs": "^7.2.0"
  }
}
